import { Headline600Directive } from './headline-600.directive';

describe('Headline600Directive', () => {
  it('should create an instance', () => {
    const directive = new Headline600Directive();
    expect(directive).toBeTruthy();
  });
});
