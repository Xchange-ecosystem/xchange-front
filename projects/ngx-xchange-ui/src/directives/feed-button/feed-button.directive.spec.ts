import { FeedButtonDirective } from './feed-button.directive';

describe('FeedButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new FeedButtonDirective();
    expect(directive).toBeTruthy();
  });
});
