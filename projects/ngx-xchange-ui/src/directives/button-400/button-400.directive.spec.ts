import { Button400Directive } from './button-400.directive';

describe('Button400Directive', () => {
  it('should create an instance', () => {
    const directive = new Button400Directive();
    expect(directive).toBeTruthy();
  });
});
