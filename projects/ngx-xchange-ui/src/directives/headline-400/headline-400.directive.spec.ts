import { Headline400Directive } from './headline-400.directive';

describe('Headline400Directive', () => {
  it('should create an instance', () => {
    const directive = new Headline400Directive();
    expect(directive).toBeTruthy();
  });
});
