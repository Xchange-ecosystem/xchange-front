import { NonScrollDirective } from './non-scroll.directive';

describe('NonScrollDirective', () => {
  it('should create an instance', () => {
    const directive = new NonScrollDirective();
    expect(directive).toBeTruthy();
  });
});
