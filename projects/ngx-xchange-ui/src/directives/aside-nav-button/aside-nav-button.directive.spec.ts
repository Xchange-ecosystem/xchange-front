import { AsideNavButtonDirective } from './aside-nav-button.directive';

describe('AsideNavButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new AsideNavButtonDirective();
    expect(directive).toBeTruthy();
  });
});
