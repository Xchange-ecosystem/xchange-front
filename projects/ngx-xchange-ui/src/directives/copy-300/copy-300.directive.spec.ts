import { Copy300Directive } from './copy-300.directive';

describe('Copy300Directive', () => {
  it('should create an instance', () => {
    const directive = new Copy300Directive();
    expect(directive).toBeTruthy();
  });
});
