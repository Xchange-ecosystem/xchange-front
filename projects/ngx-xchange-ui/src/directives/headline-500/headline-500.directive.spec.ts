import { Headline500Directive } from './headline-500.directive';

describe('Headline500Directive', () => {
  it('should create an instance', () => {
    const directive = new Headline500Directive();
    expect(directive).toBeTruthy();
  });
});
