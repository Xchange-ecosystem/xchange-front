import { MainHeadingDirective } from './main-heading.directive';

describe('MainHeadingDirective', () => {
  it('should create an instance', () => {
    const directive = new MainHeadingDirective();
    expect(directive).toBeTruthy();
  });
});
