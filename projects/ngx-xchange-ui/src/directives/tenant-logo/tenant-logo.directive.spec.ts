import { TenantLogoDirective } from './tenant-logo.directive';

describe('TenantLogoDirective', () => {
  it('should create an instance', () => {
    const directive = new TenantLogoDirective();
    expect(directive).toBeTruthy();
  });
});
