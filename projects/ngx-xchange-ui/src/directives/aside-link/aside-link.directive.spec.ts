import { AsideLinkDirective } from './aside-link.directive';

describe('AsideLinkDirective', () => {
  it('should create an instance', () => {
    const directive = new AsideLinkDirective();
    expect(directive).toBeTruthy();
  });
});
