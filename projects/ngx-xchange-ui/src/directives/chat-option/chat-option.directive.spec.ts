import { ChatOptionDirective } from './chat-option.directive';

describe('ChatOptionDirective', () => {
  it('should create an instance', () => {
    const directive = new ChatOptionDirective();
    expect(directive).toBeTruthy();
  });
});
