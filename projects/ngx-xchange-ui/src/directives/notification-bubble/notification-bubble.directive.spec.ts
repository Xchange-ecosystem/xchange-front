import { NotificationBubbleDirective } from './notification-bubble.directive';

describe('NotificationBubbleDirective', () => {
  it('should create an instance', () => {
    const directive = new NotificationBubbleDirective();
    expect(directive).toBeTruthy();
  });
});
