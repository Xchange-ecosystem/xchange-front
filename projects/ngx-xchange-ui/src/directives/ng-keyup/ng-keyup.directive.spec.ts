import { NgKeyupDirective } from './ng-keyup.directive';

describe('NgKeyupDirective', () => {
  it('should create an instance', () => {
    const directive = new NgKeyupDirective();
    expect(directive).toBeTruthy();
  });
});
