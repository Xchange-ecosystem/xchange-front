import { Headline300Directive } from './headline-300.directive';

describe('Headline300Directive', () => {
  it('should create an instance', () => {
    const directive = new Headline300Directive();
    expect(directive).toBeTruthy();
  });
});
