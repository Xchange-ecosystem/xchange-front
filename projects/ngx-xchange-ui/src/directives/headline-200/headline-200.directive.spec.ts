import { Headline200Directive } from './headline-200.directive';

describe('Headline200Directive', () => {
  it('should create an instance', () => {
    const directive = new Headline200Directive();
    expect(directive).toBeTruthy();
  });
});
