import { ProfileHeaderDirective } from './profile-header.directive';

describe('ProfileHeaderDirective', () => {
  it('should create an instance', () => {
    const directive = new ProfileHeaderDirective();
    expect(directive).toBeTruthy();
  });
});
