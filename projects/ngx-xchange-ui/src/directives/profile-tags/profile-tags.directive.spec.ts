import { ProfileTagsDirective } from './profile-tags.directive';

describe('ProfileTagsDirective', () => {
  it('should create an instance', () => {
    const directive = new ProfileTagsDirective();
    expect(directive).toBeTruthy();
  });
});
