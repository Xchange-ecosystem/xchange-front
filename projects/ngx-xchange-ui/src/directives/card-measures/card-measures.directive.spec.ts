import { CardMeasuresDirective } from './card-measures.directive';

describe('CardMeasuresDirective', () => {
  it('should create an instance', () => {
    const directive = new CardMeasuresDirective();
    expect(directive).toBeTruthy();
  });
});
