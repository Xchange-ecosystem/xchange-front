import { AsideNavDirective } from './aside-nav.directive';

describe('AsideNavDirective', () => {
  it('should create an instance', () => {
    const directive = new AsideNavDirective();
    expect(directive).toBeTruthy();
  });
});
