import { ButtonViewDirective } from './button-view.directive';

describe('ButtonViewDirective', () => {
  it('should create an instance', () => {
    const directive = new ButtonViewDirective();
    expect(directive).toBeTruthy();
  });
});
