import { Button500Directive } from './button-500.directive';

describe('Button500Directive', () => {
  it('should create an instance', () => {
    const directive = new Button500Directive();
    expect(directive).toBeTruthy();
  });
});
