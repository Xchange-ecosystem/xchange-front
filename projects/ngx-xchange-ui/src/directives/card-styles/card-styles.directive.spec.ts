import { CardStylesDirective } from './card-styles.directive';

describe('CardStylesDirective', () => {
  it('should create an instance', () => {
    const directive = new CardStylesDirective();
    expect(directive).toBeTruthy();
  });
});
