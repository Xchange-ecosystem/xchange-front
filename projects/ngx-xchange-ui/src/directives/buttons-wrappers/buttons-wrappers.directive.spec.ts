import { ButtonsWrappersDirective } from './buttons-wrappers.directive';

describe('ButtonsWrappersDirective', () => {
  it('should create an instance', () => {
    const directive = new ButtonsWrappersDirective();
    expect(directive).toBeTruthy();
  });
});
