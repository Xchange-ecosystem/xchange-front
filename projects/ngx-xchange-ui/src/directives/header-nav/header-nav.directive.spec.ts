import { HeaderNavDirective } from './header-nav.directive';

describe('HeaderNavDirective', () => {
  it('should create an instance', () => {
    const directive = new HeaderNavDirective();
    expect(directive).toBeTruthy();
  });
});
