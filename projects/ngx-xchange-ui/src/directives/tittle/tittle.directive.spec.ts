import { TittleDirective } from './tittle.directive';

describe('TittleDirective', () => {
  it('should create an instance', () => {
    const directive = new TittleDirective();
    expect(directive).toBeTruthy();
  });
});
